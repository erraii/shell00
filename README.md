# shell00
This is the first C Piscine

# o be used at the evaluation
go to home -> evaluation and run in terminal
git clone * name
tar -xvf testShell00.tar
tar -xvf exo2.tar
ls -l
bash git_commit.sh | cat -e
bash git_ignore.sh | cat -e (run on root folder)
copy a and sw_org.diff
diff a b > sw.diff
touch "~" "a~" "~b" "#c#" "#d" "#e"
